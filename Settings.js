
export const offset = 0.2;
export const scale = 0.5;
export const attackScale = 1;
export const speedScale = 1;
export const frame = 30;
export const tileSize = 50;

export const columns = Math.floor(innerWidth / tileSize) + 1;
export const rows = Math.floor(innerHeight / tileSize) + 1;